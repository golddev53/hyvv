overwrite: true
schema: "http://localhost:4000/graphql"
documents: "utils/graphql/**/*.graphql"
generates:
  utils/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
